# Phase 2.4 Codex Dispatch Sheet

Workflow: paste one prompt into Codex, let it finish and commit, then return
to the Claude session for review BEFORE pasting the next prompt. Claude
independently re-runs the gates, reads the diff, and (after Tasks 4 and 5)
runs the automated screenshot verification. Do not dispatch two tasks at
once — later tasks consume interfaces from earlier ones.

Plan: `docs/superpowers/plans/2026-07-03-phase2.4-help-about.md`
All prompts assume the repo root `d:\AI\Rift\signal-bridge-live-gui` as the
working directory. Baseline before Task 1: commit `2e316f3`, 34 tests passing.

---

## Task 1 prompt

```
Execute Task 1 from docs/superpowers/plans/2026-07-03-phase2.4-help-about.md
(Phase 2.4: markdown_view). Create ONLY sb_ui/markdown_view.py and
tests/test_markdown_view.py; do not touch any other file. Follow the TDD
step order exactly as written; the plan contains the complete code and
tests — use them verbatim. Gates before committing:
python -m py_compile signal_bridge_gui.py; python scripts/check-fixtures.py;
pytest tests/ -q (42 passed expected). Commit with the plan's Step 5 commit
message. Report: commit hash, test count, and any deviation you had to make.
```

## Task 2 prompt

```
Execute Task 2 from docs/superpowers/plans/2026-07-03-phase2.4-help-about.md
(Phase 2.4: sb_help + help docs). Create ONLY sb_help.py, the nine files
under docs/help/, and tests/test_help_content.py; do not touch any other
file. The plan contains the complete module, tests, and the full markdown
content of all nine docs — use them verbatim (do not rewrite or "improve"
the prose). Gates before committing: python -m py_compile
signal_bridge_gui.py; python scripts/check-fixtures.py; pytest tests/ -q
(45 passed expected). Commit with the plan's Step 6 commit message.
Report: commit hash, test count, and any deviation.
```

## Task 3 prompt

```
Execute Task 3 from docs/superpowers/plans/2026-07-03-phase2.4-help-about.md
(Phase 2.4: SettingsShell nav_title/show_apply). Modify ONLY
sb_ui/settings_center.py and tests/test_settings_center.py, exactly as the
plan specifies — the change must stay backward compatible (defaults
nav_title="Settings", show_apply=True). Gates before committing:
python -m py_compile signal_bridge_gui.py; python scripts/check-fixtures.py;
pytest tests/ -q (47 passed expected). Commit with the plan's Step 5 commit
message. Report: commit hash, test count, and any deviation.
```

## Task 4 prompt

```
Execute Task 4 from docs/superpowers/plans/2026-07-03-phase2.4-help-about.md
(Phase 2.4: Help window + Help menu + Recognition Rules hook). Modify ONLY
signal_bridge_gui.py. The plan gives exact code for: two new URL constants
next to UPDATE_RELEASE_URL; import sb_help and
`from sb_ui import markdown_view as sb_markdown`; the show_help_center
method (insert directly before `def show_about`); the replacement Help menu
block (locate `help_menu = tk.Menu(menubar, ...)`); and a "?" button in
show_esi_exclusion_list right after its polish_window call. Line numbers
in the plan were verified at commit 986aee0 — locate by the quoted snippets
if they have drifted. Do not attempt the screenshot verification step; the
reviewer runs it. Gates before committing: python -m py_compile
signal_bridge_gui.py; python scripts/check-fixtures.py; pytest tests/ -q
(47 passed expected). Commit with the plan's Step 6 commit message.
Report: commit hash, test count, and any deviation.
```

## Task 5 prompt

```
Execute Task 5 from docs/superpowers/plans/2026-07-03-phase2.4-help-about.md
(Phase 2.4: About window). Modify ONLY signal_bridge_gui.py. The plan gives
exact code for: the new show_about_window method (insert directly before
`def show_about`); replacing the ENTIRE bodies of show_about and
show_support with one-line calls to show_about_window; re-pointing the Help
menu "About Signal Bridge..." entry to self.show_about_window; and the
slimmed _render_settings_about. IMPORTANT: keep the footer packed with
side="bottom" BEFORE the body frame is packed, exactly as the plan's code
does — pack order is clip priority. Do not attempt the screenshot
verification step; the reviewer runs it. Gates before committing:
python -m py_compile signal_bridge_gui.py; python scripts/check-fixtures.py;
pytest tests/ -q (47 passed expected). Commit with the plan's Step 5 commit
message. Report: commit hash, test count, and any deviation.
```

## Task 6 prompt

```
Execute Task 6 from docs/superpowers/plans/2026-07-03-phase2.4-help-about.md
(Phase 2.4: tracker closure). Modify ONLY ISSUES.md, CHANGELOG.md, and
GITHUB_RELEASE.md, exactly as the plan specifies (two issue headings to
"## Fixed: ...", fix summaries, two CHANGELOG Unreleased bullets, one
packaging line in GITHUB_RELEASE.md's Intended packaged data section).
Match each file's existing formatting conventions. Sanity gate:
pytest tests/ -q (47 passed). Commit with the plan's Step 4 commit message.
Report: commit hash and any deviation.
```

---

## Reviewer protocol (Claude session, after each Codex task)

1. `git log` — confirm exactly one new commit with the plan's message.
2. Re-run the gates: `py_compile`, `check-fixtures`, `pytest tests/ -q`
   (expected counts: T1 42, T2 45, T3+ 47).
3. Read the full diff against the plan's code — flag omissions (the 2.1
   mouse-wheel lesson), scope creep, or silent rewrites of plan content.
4. After Task 4: screenshot harness — `SignalBridgeGui()` without `.run()`,
   `show_help_center("Pilot Info")`, verify nine nav topics, rendered
   markdown, Close-only footer, "Help" nav header.
   After Task 5: `show_about_window()`, verify links/actions/donation card
   and footer visibility at forced 500x440.
5. Only after the review passes: hand the next prompt to the owner.
```
